import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  areaIcon: {
    height: 50,
    width: 50,
    backgroundColor: '#3474A4',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 8,
    position: 'absolute',
    top: 10,
    left: 10,
    zIndex: 1000
  }
})

export default styles
